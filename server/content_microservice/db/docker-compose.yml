services:
  redjungle_bank:
    image: postgres:17
    container_name: content_db
    restart: always
    env_file:
      - .env
    volumes:
      - pgdata_volume:/var/lib/posgresql/data
      - ./config:/etc/postgresql/
    ports:
      - '7014:5432'
volumes:
  pgdata_volume:
  





